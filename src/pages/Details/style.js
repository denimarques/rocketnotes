import styled from "styled-components"
import tw from "twin.macro"

export const Content = styled.div`
  ${tw`w-full h-screen`};
`;

export const Container = styled.div`
    ${tw`flex flex-col max-w-xl mx-auto gap-5`};
`;

export const Links = styled.ul`
  ${tw`mt-2`};
`;
